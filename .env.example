# Copy to .env, then set TASKFLOW_JWT_KEY to the output of: openssl rand -base64 48
# Empty keys intentionally cause API startup to fail.
TASKFLOW_JWT_KEY=
API_PORT=8080
POSTGRES_PORT=5432
# Local development only. An existing volume retains its original password.
POSTGRES_PASSWORD=postgres
